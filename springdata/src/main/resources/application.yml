spring:
  datasource:
    url: ${MYSQL_URI}
    username: ${USER_NAME}
    password: ${PASSWORD}
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect